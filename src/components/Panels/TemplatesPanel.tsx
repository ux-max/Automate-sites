/* This file exists to satisfy the import statement inside SidePanelManager.tsx. We don't need a separate Templates panel as we integrated it into the ThemePanel instead. */
export default function TemplatesPanel() { return null; }
